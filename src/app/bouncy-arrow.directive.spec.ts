import { BouncyArrowDirective } from './bouncy-arrow.directive';

describe('BouncyArrowDirective', () => {
  it('should create an instance', () => {
    const directive = new BouncyArrowDirective();
    expect(directive).toBeTruthy();
  });
});
